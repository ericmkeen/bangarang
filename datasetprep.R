# Dataset prep
#pkgdir <- "/Users/eric.keen/Dropbox/Bangarang Docs/R Bangarang/packages/bangarang"
#readdir <- "/Users/eric.keen/Dropbox/Bangarang Docs/R Bangarang/packages/bangarang/data-raw"

#setwd(readdir)
#channels <- read.csv("channelpolygons.csv",stringsAsFactors=FALSE)
#setwd(pkgdir)
#devtools::use_data(channels)

#setwd(readdir)
#deadendkey <- read.csv("deadendkey.csv",stringsAsFactors=FALSE)
#setwd(pkgdir)
#devtools::use_data(deadendkey)

#setwd(readdir)
#blocks <- read.csv("blocks.csv",stringsAsFactors=FALSE)
#setwd(pkgdir)
#devtools::use_data(blocks)

#setwd(readdir)
#NodesFirstStepsMatrix <- read.csv("nodes-FirstSteps-matrix.csv",stringsAsFactors=FALSE)
#setwd(pkgdir)
#devtools::use_data(NodesFirstStepsMatrix)

#setwd(readdir)
#nodesLOSmatrix <- read.csv("nodes-LOS-matrix.csv",stringsAsFactors=FALSE)
#setwd(pkgdir)
#devtools::use_data(nodesLOSmatrix)

#setwd(readdir)
#nodesraw <- read.csv("nodes-raw.csv",stringsAsFactors=FALSE)
#setwd(pkgdir)
#devtools::use_data(nodesraw)

#setwd(readdir)
#provinces <- read.csv("provpolygons.csv",stringsAsFactors=FALSE)
#setwd(pkgdir)
#devtools::use_data(provinces)

#setwd(readdir)
#shiplane <- read.csv("shiplane.csv",stringsAsFactors=FALSE)
#setwd(pkgdir)
#devtools::use_data(shiplane)

#setwd(readdir)
#stations <- read.csv("stations2014.csv",stringsAsFactors=FALSE)
#setwd(pkgdir)
#devtools::use_data(stations)

#setwd(readdir)
#subblocks <- read.csv("triblocks.csv",stringsAsFactors=FALSE)
#setwd(pkgdir)
#devtools::use_data(subblocks)

#setwd(readdir)
#waterpolygons <- read.csv("waterpolygons.csv",stringsAsFactors=FALSE)
#setwd(pkgdir)
#devtools::use_data(waterpolygons)
